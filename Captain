import datetime

def notes():
    notesdict = {}
    while True:
        inp = input()
        if inp == 'q':
            break
        notesdict[str(datetime.date.today())] = inp

    return notesdict
print(notes())